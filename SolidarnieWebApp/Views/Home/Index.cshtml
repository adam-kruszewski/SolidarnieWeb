@{
    
}

<div class="jumbotron">
    <h1>ASP.NET</h1>
    <h2>@ViewBag.Title</h2>
</div>

<div class="row">
    <a href="@Url.Action("UtworzBaze")">Utwórz nową bazę danych.</a>
    @if (!string.IsNullOrEmpty(ViewBag.Komunikat))
    {
        <span class="alert-info">@ViewBag.Komunikat</span>
    }
</div>